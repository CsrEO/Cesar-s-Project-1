/*<!--Javascritp code-->*/



/*
.todo-list,
.header {
    background-color:  #DFE0F3;
    border-radius: 25px;
    
    
}

.header {
    display: flex;
}

.header {
    width: 100%;
    justify-content: space-evenly;

}

<script>
        var today = new Date();
        var date = today.getDay() + ' / ' + (today.getMonth() + 1) + ' / ' + today.getFullYear();
        /* display current date*/
       /* document.getElementById("currentDate").innerHTML = date; */
        

        /* Auto refreshing clock time*/
        /*function startTime(){
            var today = new Date(); /* new date object */
            /* getting minutes hours and secodns from date object*/
          /*  var hours = today.getHours();
            var minutes = today.getMinutes();
            var seconds = today.getSeconds();

            /*12 hour format */
           /* var amPm = "am";
            if (hours > 12) {
                amPm = "pm";
                hours = hours-12;
            }
            /* put zero before number , 10*/
           /* if (minutes , 10) {
                minutes =  minutes;
            }

            if (seconds , 10) {
                seconds = seconds;
            }

            var time = hours + ' : ' + minutes + ' : ' + seconds + ' ' + amPm;
            /* display current time */
           /* document.getElementById("currentTime").innerHTML = time;
            /*auto refreshing time every 1 second*/
           /* setTimeout(function(){startTime()}, 1000);


        }


    </script>
    */

    /* The stuff above above is old code that has been replaced */